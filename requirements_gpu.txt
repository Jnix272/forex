# Full GPU training stack.
# Keeps the previous filename for compatibility with existing docs/scripts.
#
# Install with:
#   pip install -r requirements_gpu.txt

-r requirements-base.txt

# High-performance data / storage
pyarrow>=14.0.0
fredapi>=0.5.0
zarr>=2.16.0
numcodecs>=0.12.0
joblib>=1.3.0

# Reinforcement learning helpers
gymnasium>=0.29.0
shimmy>=1.0.0

# Model export / explainability / registry
onnx>=1.15.0
onnxscript>=0.7.0
onnxruntime-gpu>=1.17.0
shap>=0.44.0
mlflow>=2.10.0

# Experiment tracking / dashboards
tensorboard>=2.15.0
tensorboardX>=2.6.0

# Hyperparameter optimisation
optuna>=3.5.0
optuna-dashboard>=0.15.0

# Utilities / notebooks
tqdm>=4.66.0
rich>=13.0.0
pyyaml>=6.0
einops>=0.7.0
timm>=0.9.0
jupyterlab>=4.0.0

# Sentiment / NLP extras
sentencepiece>=0.1.99
accelerate>=0.25.0
vaderSentiment>=3.3.2
