# Deep Audit — Forex Scaling Model (2026-08-06)

**Scope:** Whole codebase (~116k LOC Python, 91 test files), all categories.
**Method:** 8 parallel forensic sweeps (forex-scaling agents) + targeted verification (ruff F821 analysis, `bind_host` injection tracing, runtime probes).
**Baselines at audit time:** pyright 0 errors / 70 warnings; ruff **1619 findings** (incl. 220 F821 undefined-name, 123 F841, 18 F811). Many modules carry a large **uncommitted diff** (77 files, ~5k insertions) — findings introduced there are flagged **[DIFF]**.

**Recheck (2026-08-06):** probes against current tree. **OPEN** = still true · **PARTIAL** = mitigated, not closed · **DONE** = fixed (not an open finding).

> **DONE (not open):** `SessionLimitsEnforcer` wired in `live_engine` (~L1343+); DST session names via `trading/session_utils`; shared `session_spread_mult` on Python backtest fills.
> **FIXED 2026-08-06:** M1 broker-side SL/TP (OANDA/MT5/IBKR) + M2 confirmed-fill position tracking; training fixes T2 (`_scaler_npz_path` import), T4 (`or 15`→30 embargo), direction_control `sanitize_array`; RL-labeling L2 (tail-cut), L3 (None session→1.0), L4 (`low_vol`), L5 (warm-up div).

### Recheck scoreboard

| ID | Status | Probe note |
|----|--------|------------|
| M1 | **OPEN** | No `stopLossOnFill` / broker SL/TP in bridge |
| M2 | **OPEN** | `market_order` return discarded; `_position = lots` at ~1381/1389 |
| M3–M7 | **OPEN** | Lot convention, feed `pass`, VaR, `on_trade_closed`, cumulative book |
| B1 | **PARTIAL** | Numba ≥50k; equity-curve metrics fallback — **trade log still empty** |
| B2–B5 | **OPEN** | Mid fills, JPY MTM, meta in-sample, advanced-exec import |
| D1–D6 | **OPEN** | PIT / regime-gate / scaler / no-trade / bfill / obi_proxy |
| L1 | **OPEN** | Bayesian key mismatch (dead path) |
| L2 | **OPEN** | Tail-cut vs `_max_h` |
| L3 | **OPEN** | `resolve_session_cost(None) == 1.5` (`None`→`off`) |
| L4–L5 | **OPEN** | `low_vol` branch; warm-up slip |
| T1–T6 | **OPEN** | Cache hash, `_scaler_npz_path`, dual loaders, embargo `or 15`, RegimeCV, purged_cv dead |
| C1 | **OPEN** | `SECTION_MAP["risk"]="RISK"` (not `LIVE_RISK`) |
| C2 | **OPEN** | `config/run.yaml` → `quick.enabled: true` |
| C3–C6 | **OPEN** | Schema gaps, unused specs, session catalog drift, dead YAML keys |
| P1–P5 | **OPEN** | Weak retrain promote, gate fail-open, drift sample/combine, audit unwired |
| S1–S6 | **OPEN** | Hygiene (P3) |

---

## 1. Priority findings — money-loss / live-trading (P0/P1)

| # | Sev | Status | Location | Problem | Fix | Verif |
|---|---|---|---|---|---|---|
| M1 | **P0** | **DONE** | `trading/live_engine.py:762-...` | **No broker-side SL/TP** — FIXED: `market_order(..., stop_loss=, take_profit=)` threads ATR-based SL/TP into OANDA `stopLossOnFill`/`takeProfitOnFill`, MT5 `sl`/`tp`, and IBKR parent/child orders at entry (`_on_new_bar`, ~L1394-1440). | Attach broker-native SL/TP at entry for every venue. | fixed 2026-08-06 |
| M2 | **P0** | **DONE** | `trading/live_engine.py:~1412` | **Order result ignored → phantom positions** — FIXED: `_place()` helper checks `r.get("ok") is False` (BridgeBrokerAdapter reject) and returns without updating `_position`; journals `order_rejected`. OANDA raises on HTTP error; PaperBroker `ok:True`. | Check `ok`/exceptions; set `_position` only on confirmed fill; reconcile from `get_positions()`. | fixed 2026-08-06 |
| M3 | **P1** | **OPEN** | `trading/live_engine.py:754`, `execution/broker_bridge.py:156,178` | **Lot/notional convention mismatch per venue.** Same `lots` ⇒ OANDA 10k units, MT5 standard 100k lots (~10× notional), IBKR base-currency units (~negligible). MT5/IBKR adapter is mis-sized by design. **[DIFF]** | Convert per venue or unify convention (MT5 `lots/10`, IBKR `lots*100000`). | confirmed |
| M4 | **P1** | **OPEN** | `trading/live_engine.py:1055-1064` | **Feed/tick outage silently freezes all risk logic.** `except Exception: pass` around `get_bid_ask`; ATR stop, equity-stall halt, drawdown, circuit-breakers only fire inside `_on_new_bar` which needs new bars. No failure counter / heartbeat / flatten-on-disconnect. | Track consecutive failures, hard stall timeout that halts/flattens; raise on sustained error. | confirmed (control-flow trace) |
| M5 | **P1** | **OPEN** | `risk/execution.py:230,273-282` + `trading/live_engine.py:1311` | **Portfolio VaR de-risk gate is numerically inert.** `parametric_var` mixes price-fraction returns with pip-dollar weights ⇒ `var_usd ≈ $0.025`, `var_pct ≈ 2e-7`; `size_adj=0.5` (threshold 0.02) never fires. **Also `max_allowed_lots` fail-open**: `np.clip(...,0.01,10.0)` returns 10.0 lots for any low-history/zero-budget pair. | Compute covariance in dollar space (lots·notional·frac_ret); tie max-allowed-lots to a dollar budget. | confirmed by execution |
| M6 | **P1** | **OPEN** | `trading/live_engine.py` wiring; `risk/risk_engine.py:249` | **`RiskEngine.on_trade_closed`/position book never fed on live path.** Only `check_order`/`update_equity`/`new_day` are called; `day_realized_pnl`, `consecutive_losses`, `realized_pnl` stay 0 forever ⇒ daily-loss gate, max-consecutive-loss, and risk-engine daily-loss circuit-breaker are no-ops live. | Call `on_trade_closed(pnl=..., equity=...)` at close site (mirror `dae.update` at live_engine.py:1234). | confirmed (grep + simulation) |
| M7 | **P1** | **OPEN** | `trading/live_engine.py:1364` | **Pre-trade limits are per-order, not cumulative portfolio.** `check_order` compares the single order's `lots`/notional against caps; `RiskEngine.positions` is always `{}` live ⇒ concentration/max_lots/max_notional never bound the aggregate open book. | Track open book via `open_position`/`close_position`; check existing+incoming cumulative. | confirmed (REPL) |

---

## 2. Priority findings — backtest realism (P0/P1)

| # | Sev | Status | Location | Problem | Fix | Verif |
|---|---|---|---|---|---|---|
| B1 | **P1** | **PARTIAL** | `backtesting/backtest.py` Numba path + `performance_metrics` | **Numba core (default ≥50k bars) drops ALL trade records** ⇒ `n_trades=0`, `win_rate=0`, empty trades CSV, misleading OOS decision artifact. **[DIFF]** Equity-curve fallback avoids KeyError but does **not** restore trade-level metrics. | Populate trade log on Numba path or refuse when trade metrics needed; never silently return 0 trades. | recheck: `_NUMBA_MIN_BARS` + `_equity_curve_metrics` |
| B2 | **P1** | **OPEN** | `backtesting/backtest.py:807-809,380-774` | **Numba path books fills at mid (no spread) when bid/ask columns absent + skips market impact on closes** ⇒ systematically optimistic (+0.34% in test) vs Python path, silently. **[DIFF]** | Fall back to `close ± 0.5×spread`; add impact on close legs consistently. | confirmed |
| B3 | **P1** | **OPEN** | `backtesting/backtest.py:694,897` | **JPY-pair MTM ~100× wrong.** Unrealised PnL uses `0.01×10000=$100/lot` while realised uses `pip_value_per_lot=1.0` (`$1/pip/lot`); `pip_value_per_lot` never set by any caller ⇒ inflated equity/drawdown/Sharpe for USDJPY/EURJPY/GBPJPY. | Derive per-pair `pip_value_per_lot`; align unrealised with realised basis. | confirmed (USDJPY: unrealised -$62 vs realised -$1.2) |
| B4 | **P1** | **OPEN** | `scripts/backtest_model.py:241-290,595` | **Meta-labeler trained & applied in-sample on same window with future triple-barrier outcomes** ⇒ look-ahead inflates win rate when `--meta-labeling`. | Fit on strictly earlier sub-window, apply to later unseen window (purge/embargo). | suspected→logic confirmed |
| B5 | **P2** | **OPEN** | `scripts/backtest_model.py:400` | **`--execution-engine advanced` is silently dead**: imports `AdverseSelectionModel,LatencyModel` from `execution.realism` (they live in `backtesting/execution.py:438,534`); `except Exception` swallows it → falls back to flat slippage. Failing test `test_backtest_wiring.py::test_advanced_execution_overlay_advanced`. **[DIFF]** | Fix import; log failure instead of silent fallback. | confirmed (test fails) |

---

## 3. Priority findings — data leakage / feature correctness (P0/P1)

| # | Sev | Status | Location | Problem | Fix | Verif |
|---|---|---|---|---|---|---|
| D1 | **P1** | **OPEN** | `data/cross_asset.py:138`, `data/eodhd.py:~600`, `data/feature_materializers.py:342`, `features/feature_engineering_pl.py:1146` | **Daily macro/cross-asset series timestamped 00:00 UTC leak same-day close into bars** — a 00:05 bar uses a VIX/DXY close that won't exist for ~21h. Largest PIT leak. | Reindex daily rows to `date+1day 00:00` (or true close time + delay). | confirmed (probe: parsed 00:00 UTC) |
| D2 | **P1** | **OPEN** | `features/feature_engineering_pl.py:1377-1390` | **`RegimeGateClassifier` fit on whole future frame** (fit on first 70% of ALL rows, predict on every bar) ⇒ earliest 30% predicted by a model trained on later bars; middle 70% in-sample. | Causal expanding-window fit or per-fold OOS-only predict. | confirmed |
| D3 | **P1** | **OPEN** | `training/dataset_builder.py:1850,1854` | **StandardScaler fit on FULL dataset, applied to every CV fold** — validation/holdout distribution stats leak into train; also affects RL price reconstruction. | Fit per-fold on `train_idx` only. | confirmed (code) |
| D4 | **P1** | **OPEN** | `features/no_trade_zones.py:359` | **Heuristic no-trade score uses full-series (future) ADX quantile** ⇒ `no_trade_score` at bar t depends on future ADX. | Causal rolling quantile. | confirmed |
| D5 | **P2** | **OPEN** | `features/feature_engineering_pl.py:2256,1210`, `features/multipair.py:233` | **Leading-window `.fill_null("backward")` pulls future values onto head rows.** | Keep forward-only / zero-fill denominators; drop backward. | confirmed |
| D6 | **P2** | **OPEN** | `data/feature_materializers.py:183-186` | **`obi_proxy` uses `tick_volume` (columns have `volume`)** ⇒ ColumnNotFoundError on schema path; `(shift(1)-cur)` sign also inverted. | Fix column name + sign. | confirmed |

---

## 4. Priority findings — labeling (P1/P2)

| # | Sev | Status | Location | Problem | Fix | Verif |
|---|---|---|---|---|---|---|
| L1 | **P1** | **OPEN** | `labeling/triple_barrier_meta.py:434-450,263-272` | **Bayesian-optimized barriers crash/never apply**: `best_params` keys `profit_mult`/`stop_mult` fed as `**kwargs` to `compute_triple_barrier_labels` which accepts `profit_atr_mult`/`stop_atr_mult` ⇒ TypeError when `bayesian_search=True` (dead path — no callers). | Rename keys to match kwargs. | static |
| L2 | **P2** | **OPEN** | `labeling/rl_reward_labeling.py:576,742-743` | **Regime tail-cut vs loop-bound mismatch** ⇒ ~23 fabricated all-zero reward/label rows per chunk (loop bound `n-_max_h-delay`; cut uses `-(lookahead+delay)`). **[DIFF]** | Cut `-(_max_h+delay)`. | confirmed (repro: 23 zero rows / 200) |
| L3 | **P2** | **OPEN** | `labeling/rl_reward_labeling.py` `resolve_session_key` / `resolve_session_cost` | **Missing `session_col` ⇒ 1.5× transaction cost on ALL bars** (`None` → key `off` → scale 1.5). **[DIFF]** | Default missing session to 1.0, not `off`. | recheck: `resolve_session_cost(None)==1.5` |
| L4 | **P2** | **OPEN** | `labeling/rl_reward_labeling.py:583-599,503-518` | **`low_vol` regime never selected when `regime_class` present** (falls to `normal` barriers). **[DIFF]** | Add `elif "low" in rv` branch. | confirmed (simulated) |
| L5 | **P2** | **OPEN** | `labeling/rl_reward_labeling.py:363-365` | **Rolling warm-up divides by ~0** ⇒ slippage multiplier ≈1.5e9 on first ~9 bars → massive negative rewards. | `fillna(1.0)` / `np.where(med_atr<=0,1.0,...)`. | confirmed (repro) |

---

## 5. Priority findings — training / model / splits (P1/P2)

| # | Sev | Status | Location | Problem | Fix | Verif |
|---|---|---|---|---|---|---|
| T1 | **P2** | **OPEN** | `training/cache_integrity.py:448-587` + `training/dataset_builder.py:3236-3248` | **Cache reuse validated by shape/length only, no feature-content hash** ⇒ stale/wrong features silently reused after any same-shape rebuild. **[DIFF partial]** | Persist+check content hash (feature_list + builder version + args). | confirmed (code) |
| T2 | **P2** | **OPEN** | `training/cache_integrity.py:838` | **`_scaler_npz_path` genuinely undefined** (not in HOST_DEPS, not imported/defined in module) ⇒ NameError on corrupt-cache cleanup path. | Import from `gpu_cache_io` or add to HOST_DEPS. | confirmed (standalone import: `hasattr=False`) |
| T3 | **P2** | **OPEN** | `training/gpu_datasets.py:97-151` vs `training/memory_management.py:60-120` | **Duplicate dataset/loader logic with incompatible NPY path conventions** — `memory_management` looks for `_cache_path/_X.npy` inside dir, mismatching sibling `_x_path` naming; only `PrioritizedDataLoader` used. | Consolidate on `gpu_datasets`; delete legacy. | confirmed |
| T4 | **P2** | **OPEN** | `training/cv_splits.py:93` | **Embargo fallback `or 15` contradicts new 30 default** ⇒ GPU path (30) vs XGBoost path (15) diverge. **[DIFF]** | Use `or LABELING.get("lookahead_bars", 30)`. | confirmed |
| T5 | **P3** | **OPEN** | `validation/cv.py:411-414` | **`RegimeCV` shuffles within regimes** breaking temporal order — dormant (not wired to production), but the most dangerous latent splitter. | Guard/wire or remove. | confirmed (code) |
| T6 | **P3** | **OPEN** | `validation/purged_cv.py:57-66` | **New post-test-embargo logic lands on dead code** — `PurgedEmbargoCV` referenced only by tests; production uses `cv_splits._embargo_split`. **[DIFF]** | Wire into production split path or remove. | confirmed (import grep) |

**Correction to prior claim:** The 220 F821 "undefined names" in `training/dataset_builder.py` / `cache_integrity.py` / `direction_control.py` are **mostly NOT runtime crashes** — they are injected at runtime by the `bind_host` monolith pattern (`_HOST_DEPS` includes `_log_warn`, `_market_bar_arrays_from_feats`, `_clamp_n_samples_to_disk`, `_get_pairs`). They remain a fragile-maintainability problem (module unusable standalone, cycle-dependent) and are **P2/P3**, with the notable **exception of `_scaler_npz_path` (T2)** which is genuinely unresolvable.

---

## 6. Config / schema drift (P1/P2)

| # | Sev | Status | Location | Problem | Fix | Verif |
|---|---|---|---|---|---|---|
| C1 | **P1** | **OPEN** | `config/config_mismatch_audit.py:17` vs `training/gpu_cli.py:462` | **Risk section audited against wrong dict** — audit compares YAML `risk:` vs `settings.RISK`, but the runtime consumer is `LIVE_RISK` ⇒ risk config never effectively audited (perpetual noise + no fail-closed on kelly/drawdown/session knobs). | Point `SECTION_MAP["risk"]="LIVE_RISK"`. | recheck: still `"RISK"` |
| C2 | **P1** | **OPEN** | `config/run.yaml:482` | **`quick.enabled: true` in the "canonical" run.yaml silently disables ensemble/RL and truncates epochs** (gpu_cli.py:1351-1359) contradicting docs and run_ubuntu.yaml. | Set `quick.enabled: false` or align docs. | recheck: still `true` |
| C3 | **P2** | **OPEN** | `config/config_schema.py` | **Validates only 3 of 30+ dicts; never validates run.yaml** ⇒ unknown/typo YAML keys (`risk.var_max_pct`, `risk.pip_value`) pass silently; `LiveRiskSchema(**LIVE_RISK)` can TypeError (only ImportError caught). | Validate loaded YAML; widen coverage; catch TypeError/ValueError. | confirmed |
| C4 | **P2** | **OPEN** | `config/settings.py:635,647,579,232,216,592` | **Six declared "spec" dicts (RETRAINING, INFRA, FILTERS, SENTIMENT, CROSS_ASSET, LATENCY) have zero non-test consumers** — schema lies about behavior. | Wire or delete/mark doc-only. | confirmed (grep) |
| C5 | **P2** | **OPEN** | `data/feature_definitions.py:255-290` vs `data/feature_materializers.py:261-271` vs `trading/session_utils.py` | **Session-feature schema drift**: catalog (london 7-16, ny 13-22, overlap 13-16) ≠ materializer (7-14, 13-20, no overlap materializer) ≠ DST-aware SoT. `session_overlap_lon_ny` declared but unmaterializable. | Single source of truth; materialize overlaps. | confirmed (read all three) |
| C6 | **P2** | **OPEN** | `config/run.yaml:510,523` | **`risk.pip_value` / `risk.var_max_pct` YAML keys have no consumer** — operators may believe a VaR cap/notional control is active when it isn't. | Wire or drop; strict unknown-key warning. | confirmed (grep) |

---

## 7. Promotion / drift / retraining (P1/P2)

| # | Sev | Status | Location | Problem | Fix | Verif |
|---|---|---|---|---|---|---|
| P1 | **P1** | **OPEN** | `retraining/orchestrator.py:42-44,255-279,467-485` | **Retrain orchestrator auto-promotes via a gate far weaker than production** (val_sharpe≥0.5, pf≥1.3, dd≤0.15, loss≤1.0; no PSR/trades/efficiency) and defaults `promote_on_complete=True` → model failing the production gate can be marked PROMOTED in registry. | Call real `PromotionGate`; default `promote_on_complete=False`. | confirmed (module read) |
| P2 | **P1** | **OPEN** | `training/post_train.py:904-924` + `validation/promotion_gate.py:248-360` | **Production gate fails open across 6 gates** — `regime_pnl={}`, `transaction_costs=0`, no turnover/latency ⇒ regime/cost/DSR/stability/efficiency gates skip-pass. Only sharpe/pf/dd/calmar/ntrades/PSR actually enforced. | Fail closed on absent required inputs; pass real values. | confirmed |
| P3 | **P2** | **OPEN** | `monitoring/drift_detection.py:138-143,198-199` | **Drift flagged on tiny samples** (`len<2 → (1.0, 0.0)` → drifted=True), no min-sample guard; wired drift path feeds `orchestrator.should_retrain` (≥30% drifted) ⇒ spurious retrains. (`drift/data_drift.py` guards correctly but is unused.) | Skip sub-threshold features in `run_drift_check`. | confirmed |
| P4 | **P2** | **OPEN** | `monitoring/drift_gate.py:170-195` | **Combines global min-p with global max-D across *different* features** ⇒ spurious/missed drift verdict. | Per-feature p AND D, then aggregate. | confirmed |
| P5 | **P2** | **OPEN** | `validation/promotion_audit.py:110` | **Priority-2 promotion audit is dead in production** — referenced only by tests; live engine trusts a single `promoted:true` flag (live_engine.py:1806-1822). | Wire into deploy script; refuse when not ready. | confirmed (grep) |

---

## 8. Security & hygiene (P2/P3)

| # | Sev | Status | Location | Problem |
|---|---|---|---|---|
| S1 | **P3** | **OPEN** | `inference/onnx_inference.py:48-58`, `training/post_train.py:416-426` | `torch_load_safe` falls back to `torch.load(weights_only=False)` (full pickle) on error ⇒ untrusted-pickle RCE if a checkpoint originates outside the trusted pipeline. |
| S2 | **P3** | **OPEN** | `features/finbert_sentiment.py:87,95` | `pickle.load` on `sentiment_cache.pkl` without integrity/trust check. |
| S3 | **P3** | **OPEN** | `monitoring/prometheus_exporter.py:134` | `start_http_server(port)` binds `0.0.0.0` by default → live financial metrics on all interfaces. Pin to `127.0.0.1`. |
| S4 | **P3** | **OPEN** | root | Dead root files with zero importers: `test_polars.py`, `verify_features.py`, `check_links.py` (untracked). |
| S5 | **P3** | **OPEN** | `requirements*.txt` | No-import deps: torchvision, stable-baselines3, gymnasium, shimmy, joblib, onnxscript, tensorboard(X), timm, einops, sentencepiece, accelerate, loguru, optuna-dashboard, alpha_vantage (statsmodels: tests only). |
| S6 | **P3** | **OPEN** | `tests/test_cv.py:513`, `tests/test_ram.py:1144` | F811 duplicate test definitions silently shadow earlier ones. |

**Security verdict:** No committed secrets. `.env` (FRED_API_KEY) correctly gitignored; credentials (Discord/Slack/FRED/EODHD/OANDA/MT5) all from env vars, never literals; no `shell=True`; no untrusted `eval/exec`. `git ls-files` contains no `.env`.

---

## 9. Dead / unreferenced code (P3 hygiene, confirmed by grep)

- `trading/live_engine.py:888,947` — `AlmgrenChrissExecutor`, `ShadowModeDeployer` instantiated, never used.
- `backtesting/gpu_backtester.py` (whole module) — no callers; flat-spread default understates off-hours costs.
- `risk/portfolio_allocator.py`, `risk/portfolio_monitor.py`, `risk/fx_greeks.py` — only tests import them; production wiring absent (docstrings claim otherwise).
- `risk/risk_engine.py::resume()` — no callers.
- `training/window_loading_plan.py` — only imported by tests.
- `labeling/off_policy_rewards.py:41-44` — `_valid_index` unused.
- `features/feature_engineering_pl.py:1330` — `news_filter_expr` dead.
- `drift/data_drift.py`, `drift/model_drift.py` — mature drift pipeline unwired in production (see P3).

---

## 10. Verdict & recommended order

**Still highest value (recheck unchanged):**
1. **M1 + M2** — broker-side SL/TP + confirmed-fill position tracking (P0, live).
2. **B1/B2/B3** — Numba trade log + mid/JPY parity (P1, backtest decisions).
3. **D1/D2/D3** — cross-asset PIT, regime-gate, scaler leakage (P1, model validity).
4. **M5/M6/M7** — VaR gate, `on_trade_closed` wiring, cumulative limits (P1, risk enforcement).
5. **P1/P2** — promotion gates (P1, auto-deploy safety).
6. **L2/L3/L4** — RL labeling bugs (P2).
7. **C1/C2** — risk audit map + `quick.enabled: false` (cheap, P1).

**Reproduce / verify commands:**
```
ruff check . --select F821            # undefined names (see T2 correction)
.venv/bin/pyright                      # 0 err / 70 warn
.venv/bin/pytest tests/test_backtest_wiring.py -k advanced   # B5
.venv/bin/pytest tests/test_risk_execution.py tests/test_risk_engine.py   # M5/M6 units
```

**Corrections to sweep reports:** (a) F821 bulk is masked by `bind_host` (not crashes) — exception `_scaler_npz_path`; (b) `SessionLimitsEnforcer` wiring and session-name unification are **DONE**; (c) JPY pip-size in the standard sizer path is cosmetic (factors cancel) but misreports ATR pips / mis-sizes `min_stop_pips`; (d) B1 KeyError mitigated by equity fallback — trade-count OOS artifact remains.
