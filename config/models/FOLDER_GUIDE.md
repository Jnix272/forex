# Folder Guide: `config/models`

## Purpose
Nested folder under `config`. Runtime settings, model profiles, feature masks, and run configuration.

## Files
| File | Role | Details |
| --- | --- | --- |
| `expert.yaml` | YAML configuration | Configuration for training runs, model profiles, providers, or runtime behavior. |
| `gnn.yaml` | YAML configuration | Configuration for training runs, model profiles, providers, or runtime behavior. |
| `haelt.yaml` | YAML configuration | Configuration for training runs, model profiles, providers, or runtime behavior. |
| `mamba.yaml` | YAML configuration | Configuration for training runs, model profiles, providers, or runtime behavior. |
| `README.md` | Markdown documentation | Per-model production configs |
| `tft.yaml` | YAML configuration | Configuration for training runs, model profiles, providers, or runtime behavior. |
| `transformer.yaml` | YAML configuration | Configuration for training runs, model profiles, providers, or runtime behavior. |

_Generated by Codex to document folder contents._
