# Production overrides for TFT (Temporal Fusion Transformer).
model:
  name: tft
  hidden_size: 128
  nhead: 4
  num_layers: 2
  dropout: 0.1
training:
  lr: 1.0e-4
  grad_clip: 0.5
  seq_len: 120
  early_stop_patience: 10   # TFT converges smoothly at 1e-4 lr with 0.5 grad clip
