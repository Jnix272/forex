# Production overrides for TFT (Temporal Fusion Transformer).
model:
  name: tft
  hidden_size: 128
  nhead: 4
  num_layers: 2
  dropout: 0.1
training:
  lr: 1.0e-3
  seq_len: 60
  early_stop_patience: 6   # TFT converges fast at 1e-3 lr; short patience avoids over-running
