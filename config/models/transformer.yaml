# Production overrides for iTransformer (context model).
model:
  name: transformer
  d_model: 128
  nhead: 8
  num_layers: 3
  dim_feedforward: 256
  dropout: 0.1
training:
  lr: 1.0e-4
  seq_len: 120
