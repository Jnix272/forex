# Production overrides for GNN (cross-asset graph model).
model:
  name: gnn
  hidden_size: 64
  num_layers: 3
  node_features: 32
  nhead: 4
  dropout: 0.1
training:
  lr: 1.0e-4
  seq_len: 80
